# Backend — Wallet Ops API

NestJS + TypeORM + PostgreSQL. See the root [README](../README.md) for full setup
and the [architecture notes](../docs/ARCHITECTURE.md) for design details.

```bash
npm install
cp .env.example .env
npm run start:dev      # http://localhost:3001, docs at /docs
npm test               # unit tests
```
