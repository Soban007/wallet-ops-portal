# Backend service
PORT=3001

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=wallet
DB_PASSWORD=wallet
DB_NAME=wallet_ops

# When true, TypeORM creates/updates tables on boot (fine for this assessment).
DB_SYNCHRONIZE=true
