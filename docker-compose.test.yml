services:
  db-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: wallet
      POSTGRES_PASSWORD: wallet
      POSTGRES_DB: wallet_ops_test
    ports:
      - '55432:5432'
    tmpfs:
      - /var/lib/postgresql/data
