# Frontend — Wallet Ops Portal

Next.js (App Router) + React. Talks to the backend API defined by
`NEXT_PUBLIC_API_URL`. See the root [README](../README.md) for full setup.

```bash
npm install
cp .env.example .env
npm run dev            # http://localhost:3000
```
